import styled from "styled-components"

export const Title = () => {
    return(
        <FAQTitle>FAQs</FAQTitle>
    )
}

const FAQTitle = styled.h1`
    color: hsl(292, 42%, 14%);
    font-size: 48px;
`